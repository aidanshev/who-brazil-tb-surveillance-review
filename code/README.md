# Platform source snapshot

Materialized core modules from the authoritative Drive platform are in `src/mdrtb_surveillance/`. The authoritative upstream also contains additional modules/configuration/web assets. Reconcile against the original source manifest before calling a GitHub tag a byte-complete upstream mirror.
